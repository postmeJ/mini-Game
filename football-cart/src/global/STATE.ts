module STATE {
    export enum GAME {
        TITLE,
        READY,
        PLAY,
        PAUSE,
        PAUSE2,
        RESUME,
        OVER,
        WAITING
    }

    export enum CHAR {
        STAND,
        READY,
        JUMP,
        DIE
    }
}