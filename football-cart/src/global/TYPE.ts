module TYPE {
    export enum CTRL {
        ROCKER,
        TAP,
        MAX
    }

    export enum MODE {
        A,
        B,
        C
    }

    export enum WALL {
        SCORE,
        LINE,
        FOLLOW,
        MAX
    }
}